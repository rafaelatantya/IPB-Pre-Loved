services:
  web:
    build: .
    container_name: ipb-preloved-web
    ports:
      - "8788:8788"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    # Wrangler akan baca .dev.vars otomatis di dalam container
    environment:
      - NODE_ENV=development
      - AUTH_TRUST_HOST=true
      - NODE_OPTIONS=--max-old-space-size=1536
      # - DB_RESET=true  # Hapus tanda '#' di awal untuk paksa RESET database saat startup
    stdin_open: true
    tty: true
    restart: unless-stopped
    # Alokasi resource yang lebih ramah laptop (2GB)
    mem_limit: 2g
    mem_reservation: 1g
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8788/login"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s # Ditingkatkan karena build lumayan lama
